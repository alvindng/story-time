<%= render 'layouts/errors', :object => @contribution %>

<%= form_for [@story, @contribution] do |f| %>
  <%= f.hidden_field :image, :value => @img_url %>
  <%= f.label :sentence %>
  <%= f.text_field :sentence %>
  <%= f.label :user %>
  <%= f.text_field :user %>
  <%= f.submit  %>
<% end %>
